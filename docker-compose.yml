version: '3'
services:

  database:
    image: postgis/postgis:13-3.4-alpine
  
  cache:
    image: redis:6-alpine

  directus:
    image: directus/directus:10.7.2
    depends_on:
      - cache
      - database



